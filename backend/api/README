# API Description

This API forms the main interface for the frontend.
It handles literally all the non-admin requests right from job listings down to signup and login.

The main idea is, let Django handles model manipulation as FastAPI deals with client.